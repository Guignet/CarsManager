import { PriceOrdererPipe } from './price-orderer.pipe';

describe('PriceOrdererPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceOrdererPipe();
    expect(pipe).toBeTruthy();
  });
});
